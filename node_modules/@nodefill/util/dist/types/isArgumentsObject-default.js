"use strict";
module.exports = function isArgumentsObject(x) {
    if (!x || typeof x !== "object") {
        return false;
    }
    return (!(Symbol.toStringTag in x) &&
        Object.prototype.toString.call(x) === "[object Arguments]");
};
