"use strict";
module.exports = function isStringObject(x) {
    if (!x || typeof x !== "object") {
        return false;
    }
    try {
        String.prototype.valueOf.call(x);
    }
    catch {
        return false;
    }
    return true;
};
